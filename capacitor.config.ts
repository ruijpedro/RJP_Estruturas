import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'pt.rjp.structures',
  appName: 'RJP Structures',
  webDir: 'dist',
  backgroundColor: '#f4f6f8'
}

export default config
